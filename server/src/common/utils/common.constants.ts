// this const defines that the object is an const
export const DEFAULT_PAGE_Limit = {
  limit: 10,
  offset: 0,
};

// here const defines that properties inside the object should remain constant
//Record defines type of key and value //satisfies allows to define types of key and value also allows accessing object
//properties using . for ex : DEFAULT_PAGE_SIZE.Folder

// const  DEFAULT_DATA_LIMIT = {
//     limit:8
//     offset:0
// }
